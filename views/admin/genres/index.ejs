<div class="admin-genres">
  <div class="admin-header">
    <h1><i class="fas fa-tags"></i> Управление жанрами</h1>
    <p class="genre-management-note">
      Если в удаляемом жанре есть книги, сначала выберите жанр-приёмник. Система перенесёт все книги в выбранный жанр и только после этого удалит старый, поэтому в карточках книг не появится пустой жанр.
    </p>
  </div>
  
  <!-- Форма добавления жанра -->
  <div class="add-genre-card">
    <h3><i class="fas fa-plus-circle"></i> Добавить новый жанр</h3>
    <form action="/admin/genres/add" method="POST" class="add-genre-form">
      <div class="form-group">
        <input type="text" name="name" placeholder="Введите название жанра..." required minlength="2">
        <button type="submit" class="btn btn-primary">
          <i class="fas fa-plus"></i> Добавить жанр
        </button>
      </div>
    </form>
  </div>
  
  <!-- Список жанров -->
  <div class="genres-grid">
    <% if (genres && genres.length > 0) { %>
      <% genres.forEach(genre => { %>
        <div class="genre-card">
          <div class="genre-card-header">
            <span class="genre-id">#<%= genre.id %></span>
            <div class="genre-actions">
              <button class="genre-action-btn edit-genre" data-id="<%= genre.id %>" title="Редактировать">
                <i class="fas fa-edit"></i>
              </button>
              <% if (genre.booksCount === 0) { %>
                <form action="/admin/genres/<%= genre.id %>?_method=DELETE" method="POST" class="inline-form">
                  <button type="submit" class="genre-action-btn delete" onclick="return confirm('Удалить жанр?')" title="Удалить">
                    <i class="fas fa-trash"></i>
                  </button>
                </form>
              <% } %>
            </div>
          </div>
          
          <div class="genre-card-body">
            <!-- Отображение названия -->
            <div class="genre-name-display" id="name-display-<%= genre.id %>">
              <span class="genre-name"><%= genre.name %></span>
            </div>
            
            <!-- Форма редактирования (скрыта по умолчанию) -->
            <div class="genre-edit-form" id="edit-form-<%= genre.id %>" style="display: none;">
              <form action="/admin/genres/<%= genre.id %>/edit" method="POST" class="edit-genre-form">
                <input type="text" name="name" value="<%= genre.name %>" required minlength="2" autocomplete="off">
                <div class="edit-actions">
                  <button type="submit" class="btn-icon success" title="Сохранить">
                    <i class="fas fa-check"></i>
                  </button>
                  <button type="button" class="btn-icon danger cancel-edit" data-id="<%= genre.id %>" title="Отмена">
                    <i class="fas fa-times"></i>
                  </button>
                </div>
              </form>
            </div>
          </div>

          <% const replacementGenres = genres.filter(item => item.id !== genre.id); %>
            <% if (genre.booksCount > 0) { %>
              <form action="/admin/genres/<%= genre.id %>?_method=DELETE" method="POST" class="genre-transfer-form" data-genre-name="<%= genre.name %>" data-books-count="<%= genre.booksCount %>">
                <label for="replacement-genre-<%= genre.id %>">
                  Перед удалением перенести книги в:
                </label>
                <div class="transfer-controls">
                  <select id="replacement-genre-<%= genre.id %>" name="replacement_genre_id" required <%= replacementGenres.length === 0 ? 'disabled' : '' %>>
                    <option value="">Выберите другой жанр</option>
                    <% replacementGenres.forEach(replacement => { %>
                      <option value="<%= replacement.id %>"><%= replacement.name %></option>
                    <% }) %>
                  </select>
                  <button type="submit" class="btn-icon danger" title="Перенести книги и удалить жанр" <%= replacementGenres.length === 0 ? 'disabled' : '' %>>
                    <i class="fas fa-trash"></i>
                  </button>
                </div>
                <% if (replacementGenres.length === 0) { %>
                  <small class="transfer-warning">Создайте ещё один жанр, чтобы было куда перенести книги.</small>
                <% } else { %>
                  <small>Будет перенесено книг: <%= genre.booksCount %>.</small>
                <% } %>
              </form>
            <% } %>
          
          <div class="genre-card-footer">
            <div class="genre-stats">
              <i class="fas fa-book"></i>
              <span><%= genre.booksCount %> <%= genre.booksCount === 1 ? 'книга' : 
                                                  genre.booksCount >= 2 && genre.booksCount <= 4 ? 'книги' : 'книг' %></span>
            </div>
            <div class="genre-date">
              <i class="far fa-calendar-alt"></i>
              <%= new Date(genre.createdAt).toLocaleDateString('ru-RU') %>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="empty-state">
        <i class="fas fa-tags fa-4x"></i>
        <h3>Жанры не найдены</h3>
        <p>Добавьте первый жанр с помощью формы выше</p>
      </div>
    <% } %>
  </div>
</div>

<style>
  .admin-genres {
    padding: 2rem;
    color: white;
  }
  
  .admin-header {
    margin-bottom: 2rem;
    padding: 1.5rem;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
  }

  .genre-management-note {
    margin: 0.75rem 0 0;
    color: rgba(255, 255, 255, 0.75);
    line-height: 1.5;
  }
  
  .add-genre-card {
    background: linear-gradient(135deg, rgba(99, 102, 241, 0.2), rgba(139, 92, 246, 0.2));
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin-bottom: 2rem;
  }
  
  .add-genre-card h3 {
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }
  
  .add-genre-form .form-group {
    display: flex;
    gap: 1rem;
  }
  
  .add-genre-form input {
    flex: 1;
    padding: 0.75rem 1rem;
    border: 2px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--radius);
    background: rgba(255, 255, 255, 0.1);
    color: white;
    font-size: 1rem;
    transition: all 0.3s ease;
  }
  
  .add-genre-form input:focus {
    outline: none;
    border-color: var(--accent);
    background: rgba(255, 255, 255, 0.15);
    box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.3);
  }
  
  .genres-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(360px, 1fr));
    gap: 1.5rem;
  }
  
  .genre-card {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    overflow: hidden;
    transition: all 0.3s ease;
  }
  
  .genre-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-xl);
    border-color: var(--accent);
  }
  
  .genre-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1rem 1.5rem;
    background: rgba(0, 0, 0, 0.2);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }
  
  .genre-id {
    font-size: 0.8rem;
    opacity: 0.6;
    font-family: monospace;
  }
  
  .genre-actions {
    display: flex;
    gap: 0.5rem;
  }
  
  .genre-action-btn {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: white;
  }
  
  .genre-action-btn.edit-genre {
    background: rgba(245, 158, 11, 0.2);
    color: #f59e0b;
  }
  
  .genre-action-btn.edit-genre:hover {
    background: #f59e0b;
    color: white;
    transform: scale(1.1);
  }
  
  .genre-action-btn.delete {
    background: rgba(239, 68, 68, 0.2);
    color: #ef4444;
  }
  
  .genre-action-btn.delete:hover:not(.disabled) {
    background: #ef4444;
    color: white;
    transform: scale(1.1);
  }
  
  .genre-action-btn.disabled {
    opacity: 0.3;
    cursor: not-allowed;
  }
  
  .genre-card-body {
    padding: 1.5rem;
  }
  
  .genre-name-display {
    text-align: center;
  }
  
  .genre-name {
    font-size: 1.4rem;
    font-weight: 600;
    background: linear-gradient(135deg, #fff, #e0e0e0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
  }
  
  .edit-genre-form {
    display: flex;
    gap: 0.5rem;
  }
  
  .edit-genre-form input {
    flex: 1;
    padding: 0.5rem 0.75rem;
    border: 2px solid var(--accent);
    border-radius: var(--radius);
    background: rgba(255, 255, 255, 0.1);
    color: white;
    font-size: 1rem;
  }
  
  .edit-actions {
    display: flex;
    gap: 0.25rem;
    flex-shrink: 0;
  }
  
  .btn-icon {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    display: inline-flex;
    align-items: center;
    justify-content: center;
  }
  
  .btn-icon.success {
    background: #10b981;
    color: white;
  }
  
  .btn-icon.success:hover {
    transform: scale(1.1);
    box-shadow: 0 0 10px #10b981;
  }
  
  .btn-icon.danger {
    background: #ef4444;
    color: white;
  }
  
  .btn-icon.danger:hover {
    transform: scale(1.1);
    box-shadow: 0 0 10px #ef4444;
  }

  .genre-transfer-form {
    margin-top: 1rem;
    padding: 1rem;
    border: 1px solid rgba(239, 68, 68, 0.25);
    border-radius: var(--radius);
    background: rgba(239, 68, 68, 0.08);
  }

  .genre-transfer-form label {
    display: block;
    margin-bottom: 0.5rem;
    color: rgba(255, 255, 255, 0.85);
    font-size: 0.9rem;
    font-weight: 600;
  }

  .genre-transfer-form small {
    display: block;
    margin-top: 0.5rem;
    color: rgba(255, 255, 255, 0.65);
  }

  .transfer-controls {
    display: flex;
    gap: 0.5rem;
  }

  .transfer-controls select {
    flex: 1;
    min-width: 0;
    padding: 0.55rem 0.75rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: var(--radius);
    background: rgba(15, 23, 42, 0.95);
    color: white;
  }

  .transfer-controls .btn-icon:disabled,
  .transfer-controls select:disabled {
    opacity: 0.45;
    cursor: not-allowed;
  }

  .transfer-warning {
    color: #fca5a5 !important;
  }
  
  .genre-card-footer {
    display: flex;
    justify-content: space-between;
    padding: 1rem 1.5rem;
    background: rgba(0, 0, 0, 0.2);
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 0.85rem;
    opacity: 0.8;
  }
  
  .genre-stats i,
  .genre-date i {
    margin-right: 0.25rem;
  }
  
  .empty-state {
    grid-column: 1 / -1;
    text-align: center;
    padding: 4rem;
    background: rgba(255, 255, 255, 0.05);
    border-radius: var(--radius-lg);
  }
  
  .empty-state i {
    opacity: 0.3;
    margin-bottom: 1rem;
  }
  
  .empty-state h3 {
    margin-bottom: 0.5rem;
  }
  
  .empty-state p {
    opacity: 0.6;
  }
  
  @media (max-width: 768px) {
    .add-genre-form .form-group {
      flex-direction: column;
    }
    
    .genres-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  // Редактирование жанра
  document.querySelectorAll('.edit-genre').forEach(btn => {
    btn.addEventListener('click', function() {
      const id = this.dataset.id;
      document.getElementById(`name-display-${id}`).style.display = 'none';
      document.getElementById(`edit-form-${id}`).style.display = 'block';
    });
  });
  
  // Отмена редактирования
  document.querySelectorAll('.cancel-edit').forEach(btn => {
    btn.addEventListener('click', function() {
      const id = this.dataset.id;
      document.getElementById(`name-display-${id}`).style.display = 'block';
      document.getElementById(`edit-form-${id}`).style.display = 'none';
    });
  });

  // Удаление жанра с переносом книг в другой жанр
  document.querySelectorAll('.genre-transfer-form').forEach(form => {
    form.addEventListener('submit', function(event) {
      const replacementSelect = this.querySelector('select[name="replacement_genre_id"]');

      if (!replacementSelect.value) {
        event.preventDefault();
        replacementSelect.reportValidity();
        return;
      }

      const genreName = this.dataset.genreName;
      const booksCount = this.dataset.booksCount;
      const replacementName = replacementSelect.options[replacementSelect.selectedIndex].text;
      const message = `Удалить жанр «${genreName}» и перенести ${booksCount} книг в жанр «${replacementName}»?`;

      if (!confirm(message)) {
        event.preventDefault();
      }
    });
  });
</script>
